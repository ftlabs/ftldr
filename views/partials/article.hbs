<table>
  <tbody>
{{#each phrases}}
    <tr>
      <th><strong>{{type}}</strong></th><td>"{{text}}"</td>
    </tr>
{{/each}}
  </tbody>
</table>

<table>
  <tbody>
    <tr>
      <th><strong>UUID</strong></th><td>"{{uuid}}"</td></tr>
      <th><strong>URL</strong></th><td><a href="{{url}}">{{url}}</a></td></tr>
  </tbody>
</table>

<h1><a href="{{url}}">{{{ content.title }}}</a></h1>
<p>{{{ bodyXML }}}</p>
